version: '3.8'

services:
  tests:
    image: node:18
    working_dir: /app
    volumes:
      - ./:/app
    command: sh -c "npm install --no-audit --no-fund && npm run test:run"
    environment:
      - CI=true
